export class TestConfig {
    // global url for the tests
    baseURL: string = 'https://parabank.parasoft.com/parabank';

    //login credentials
    username: string = 'admin';
    password: string = 'admin@123';
}